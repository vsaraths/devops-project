apiVersion: v1
kind: Service
metadata:
  name: arcadex-service
spec:
  type: NodePort
  selector:
    app: arcadex
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007
